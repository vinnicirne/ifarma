project_id = "YOUR_PROJECT_ID"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[db.pooler]
enabled = false
port = 54329
pool_mode = "transaction"
default_pool_size = 20
max_client_conn = 100

[realtime]
enabled = true
# ip_version must be IPv4 or IPv6 (case sensitive often)
ip_version = "IPv4"

# [migrations] section removed as it was flagged as invalid key

# Configurações customizadas do Ifarma
[functions.activate-pharmacy-plan]
verify_jwt = false

# [db.seed]
# enabled = true
# sql_paths = ["./supabase/seeds"]

